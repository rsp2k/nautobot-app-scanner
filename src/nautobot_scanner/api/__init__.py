"""REST API package for nautobot_scanner.

Phase 6.5 (this commit) adds basic CRUD serializers + viewsets so the
Nautobot 3.x React data-table on list views actually displays records.
Phase 7 will add agent-specific endpoints (pending-scans, ingest,
checkin) with token auth and race-protection.
"""
